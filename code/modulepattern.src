var appEngine = {
    winHeight : 0,
    appName : "",

    init : function() {
        appEngine.bindUIActions();
        appEngine.winHeight = window.innerHeight;
    },

    bindUIActions : functions() {
        $("headerName").html(appEngine.appName);
    }
};