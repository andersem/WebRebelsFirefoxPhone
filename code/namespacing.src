var visma = visma || {}; // create namespace if it doesn’t exist

visma.myFunction = (function(consulting, utils, jQuery) {
    "use strict";

    consulting.feature = function() {
        console.log('my feature', consulting);
    };
})(visma.consulting, visma.utils, jQuery);
